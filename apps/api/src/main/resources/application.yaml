spring:
  application:
    name: ferrycatch-api

  datasource:
    url: jdbc:postgresql://localhost:5432/ferrycatch
#    jdbc:postgresql://db:5432/ferrycatch - for docker
    username: ferry
    password: ferry123
  flyway:
    enabled: true
    locations: classpath:db/migration